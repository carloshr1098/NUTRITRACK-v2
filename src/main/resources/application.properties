# ============================================
# DEVELOPMENT CONFIGURATION (Local only)
# In production, application-prod.properties is used
# ============================================

# Database Configuration - H2 (Development only)
spring.datasource.url=jdbc:h2:mem:nutritrackdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=bnV0cml0cmFja19zdXBlcl9zZWNyZXRfa2V5X2Zvcl9qd3RfYXV0aGVudGljYXRpb25fMjAyNA==
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:3001

# Logging
logging.level.com.nutritrack=DEBUG
logging.level.org.springframework.security=DEBUG